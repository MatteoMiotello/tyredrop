type Cart {
    id: ID!
    userId: ID!
    productItemPriceId: ID!
    user: User!
    productItemPrice: ProductItemPrice!
    quantity: Int!
}

type TotalPrice {
    value: Float!
    currency: Currency
}

type CartResponse {
    items: [Cart]!
    totalPrice: TotalPrice
}