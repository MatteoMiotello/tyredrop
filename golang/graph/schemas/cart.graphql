type Cart {
    id: ID!
    userId: ID!
    productItemId: ID!
    user: User!
    productItem: ProductItem!
    quantity: Int!
}

type TotalPrice {
    value: Float!
    currency: Currency
}

type CartResponse {
    items: [Cart]!
    totalPrice: TotalPrice
}