extend type Mutation {
    orderSupport( orderID: ID! message: String! ): Order! @userConfirmed

    confirmOrder( orderID: ID! ): Order! @userConfirmed
    payOrder( orderID: ID!, paymentMethodCode: String! ): Order! @userConfirmed
}